import AdminLoginPage from "./admin/page";

// The marketing landing page has been retired — the site root is now the
// team login. (Old landing components remain under app/components/ if needed.)
export default function Home() {
  return <AdminLoginPage />;
}
