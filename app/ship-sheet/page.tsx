// Standalone, PUBLIC Ship Sheet — the grower's phone app lifted out of the
// dashboard so it lives on its own shareable URL (/ship-sheet), with no team
// login and no workspace sidebar in front of it. It renders the exact same
// app as /dashboard/ship-sheet (single source of truth), so the two stay in
// sync. Used to demo the grower experience on its own.
export { default } from "../dashboard/ship-sheet/page";
